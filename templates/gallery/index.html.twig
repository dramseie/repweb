{% extends 'base.html.twig' %}

{% block body %}
  <div class="row">
    <div class="col-12">
      <div class="d-flex align-items-center justify-content-between mb-4">
        <h1 class="h3 mb-0">Photo Gallery</h1>
      </div>
      <div id="gallery-root"
           data-api-base="{{ path('api_galleries_index') }}"
           data-initial-slug="{{ initial_slug ?? '' }}">
      </div>
    </div>
  </div>
{% endblock %}
